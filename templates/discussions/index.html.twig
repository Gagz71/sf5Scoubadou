{% extends 'base.html.twig' %}

{% block title %}   messages {% endblock %}

{% block body %}
        <h1 class="display-4">Vos messages</h1>
    <div class="container">
        {% for discussion in adoptingRequest.discussions %}
            <h3> message {{ adoptingRequest.id }} </h3>
            {{ discussion.content[:400] ~ '...' }}
        {% endfor %}
    </div>
{% endblock %}