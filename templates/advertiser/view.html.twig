{% extends 'base.html.twig' %}

{% block title %} Détails de l'annonceur{% endblock %}

{% block body %}

<div class="jumbotron">
    <h1 class="display-4">{{advertiser.organizationName}}</h1>
    <p class="lead">This is a simple hero unit, a simple jumbotron-style component for calling extra attention to featured content or information.</p>
    <hr class="my-4">
    <p>It uses utility classes for typography and spacing to space content out within the larger container.</p>
    <p class="lead">
        <a class="btn btn-primary btn-lg" href="#" role="button">Notre site</a>
    </p>
</div>

{% for advert in listAdverts%}
    <div class="row mt-5">
        <div class="col-sm-4">
            <div class="card">
                <div class="card-body">
                    <h2 class="card-title">{{ advert.title }}</h2>
                    <p class="card-text"> - Annonceurs présents sur la plateforme - </p>
                    .setController('App\\Controller\\Admin\\AdvertiserCrudController')
                    .setAction('index') }}" class="btn btn-primary">Annonceurs</a>
                </div>
            </div>
        </div>
    </div>
{% endfor %}



